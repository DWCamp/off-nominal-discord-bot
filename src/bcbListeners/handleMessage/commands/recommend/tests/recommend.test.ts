import { handleRecommendCommand } from '../recommend';

describe('recommend', () => {
  it('should make a GET call and send a message if argument is random', () => {
    const arg = 'random';
  });
});
